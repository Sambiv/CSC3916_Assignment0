Content-Type
application/json; charset=UTF-8
# Specifies the media type of the resource. Here, it indicates the response is JSON formatted with UTF-8 character encoding.

Vary
Origin
# Indicates that the server response might vary based on the 'Origin' header in the request. Used for caching.

Vary
X-Origin
# Similar to the above, but variation is based on the 'X-Origin' header.

Referer
# This header indicates the address of the previous web page from which a request was made.

Content-Encoding
gzip
# Specifies the encoding used on the data. 'gzip' means the content is compressed using GZIP to reduce the response size.

Date
Sun, 02 Feb 2025 19:41:00 GMT
# The date and time at which the response was generated by the server.

Server
ESF
# Indicates the server software handling the request. 'ESF' refers to Google's front-end server infrastructure.

X-XSS-Protection
0
# This header is used to control the Cross-Site Scripting (XSS) filter. '0' disables the XSS filter.

X-Frame-Options
SAMEORIGIN
# Prevents the webpage from being framed by pages from a different origin, protecting against clickjacking attacks.

X-Content-Type-Options
nosniff
# Prevents browsers from MIME-sniffing a response away from the declared content-type.

Alt-Svc
h3=":443"; ma=2592000,h3-29=":443"; ma=2592000
# Indicates alternative services that the client can use to connect to the server, including HTTP/3 over port 443.

Transfer-Encoding
chunked
# Indicates that the response is sent in chunks, allowing the server to start transmitting dynamically generated content.
